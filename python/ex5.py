# replace the substring with new substring

old_string = input ('enter the string :')
substring = input (' what substring to replace? ')
replacement = input ('new substring :')
new_string = old_string.replace(substring, replacement)
print (new_string)
